{
  "name": "portfolio-tracker-tauri",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:sidecar": "bash scripts/build-sidecar.sh",
    "build:icons": "tauri icon src-tauri/app-icon.svg",
    "build:all": "bun run build:sidecar && bun run build:icons && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
